{
  "titleZh": "学习率调度与预热",
  "titleEn": "Learning Rate Schedules and Warmup",
  "promptZh": "学习率是训练中最重要的超参数——它不应是常数，而应随训练进度变化。请用 numpy 实现本课的五种学习率调度（schedule）函数，再实现一个把调度采样成曲线的工具函数。\n\n每个调度函数接收当前步 step，返回该步的学习率：\n1. constant_schedule(step, lr)：恒定学习率，任何步都返回 lr。\n2. step_decay_schedule(step, lr, step_size, gamma)：阶梯衰减，lr * gamma ** floor(step / step_size)，每隔 step_size 步乘一次 gamma。\n3. cosine_schedule(step, lr, total_steps, lr_min)：余弦退火，lr_min + 0.5*(lr - lr_min)*(1 + cos(pi*step/total_steps))；step=0 时为峰值 lr，step=total_steps 时为 lr_min；step >= total_steps 时夹紧到 lr_min。\n4. warmup_cosine_schedule(step, lr, total_steps, warmup_steps, lr_min)：前 warmup_steps 步线性预热 lr*(step/warmup_steps)，恰在 warmup_steps 步达到峰值 lr，之后用 progress=(step-warmup_steps)/(total_steps-warmup_steps) 做余弦退火到 lr_min。若 total_steps <= warmup_steps，则全程保持预热斜坡 lr*(step/max(warmup_steps,1))。\n5. one_cycle_schedule(step, lr, total_steps)：1cycle 策略，mid=max(total_steps//2,1)；前半段从 lr/25 线性升到 lr，后半段从 lr 线性降到 lr/10000。\n\n再实现 lr_curve(schedule_fn, total_steps, lr, **kwargs)：对 step=0..total_steps-1 逐步调用 schedule_fn(s, lr=lr, total_steps=total_steps, **kwargs)，返回形状为 (total_steps,) 的 numpy 浮点数组。\n\n约束：只能使用 Python 标准库（含 math）与 numpy；不要使用 torch、文件或网络。",
  "promptEn": "The learning rate is the most important hyperparameter in training — and it should not be a constant; it should change as training progresses. Implement this lesson's five learning-rate schedule functions with numpy, plus a helper that samples a schedule into a curve.\n\nEach schedule takes the current step and returns the learning rate at that step:\n1. constant_schedule(step, lr): constant LR; return lr for any step.\n2. step_decay_schedule(step, lr, step_size, gamma): step decay, lr * gamma ** floor(step / step_size) — multiply by gamma every step_size steps.\n3. cosine_schedule(step, lr, total_steps, lr_min): cosine annealing, lr_min + 0.5*(lr - lr_min)*(1 + cos(pi*step/total_steps)); it equals the peak lr at step 0 and lr_min at step total_steps; clamp to lr_min once step >= total_steps.\n4. warmup_cosine_schedule(step, lr, total_steps, warmup_steps, lr_min): linear warmup lr*(step/warmup_steps) for the first warmup_steps, reaching the peak lr exactly at step == warmup_steps, then cosine decay to lr_min using progress = (step - warmup_steps) / (total_steps - warmup_steps). If total_steps <= warmup_steps, stay on the warmup ramp the whole time: lr*(step/max(warmup_steps,1)).\n5. one_cycle_schedule(step, lr, total_steps): the 1cycle policy. mid = max(total_steps // 2, 1); ramp linearly from lr/25 up to lr over the first half, then from lr down to lr/10000 over the second half.\n\nAlso implement lr_curve(schedule_fn, total_steps, lr, **kwargs): call schedule_fn(s, lr=lr, total_steps=total_steps, **kwargs) for s = 0..total_steps-1 and return a float np.ndarray of shape (total_steps,).\n\nConstraints: use only the Python standard library (including math) and numpy; no torch, files, or network.",
  "starter": "import math\nimport numpy as np\n\n\ndef constant_schedule(step, lr=0.01, **kwargs):\n    \"\"\"Constant learning rate: return lr for every step.\n\n    Args:\n        step: int, the current training step (ignored here).\n        lr: float, the fixed learning rate.\n    Returns:\n        float, always lr.\n    \"\"\"\n    # TODO: return the learning rate unchanged\n    raise NotImplementedError\n\n\ndef step_decay_schedule(step, lr=0.1, step_size=100, gamma=0.1, **kwargs):\n    \"\"\"Step decay: cut the lr by a factor `gamma` every `step_size` steps.\n\n    lr(step) = lr * gamma ** floor(step / step_size)\n\n    Example: lr=0.1, step_size=10, gamma=0.1 gives 0.1 for steps 0..9,\n    0.01 for steps 10..19, 0.001 for steps 20..29, and so on.\n\n    Args:\n        step: int, the current step.\n        lr: float, the base (starting) learning rate.\n        step_size: int, how many steps between each drop.\n        gamma: float, the multiplicative decay factor.\n    Returns:\n        float, the decayed learning rate.\n    \"\"\"\n    # TODO: return lr * gamma ** (step // step_size)\n    raise NotImplementedError\n\n\ndef cosine_schedule(step, lr=0.01, total_steps=1000, lr_min=1e-5, **kwargs):\n    \"\"\"Cosine annealing from lr (at step 0) down to lr_min (at step total_steps).\n\n    lr(step) = lr_min + 0.5 * (lr - lr_min) * (1 + cos(pi * step / total_steps))\n\n    At step 0 the cosine term is 1, so the result is lr (the peak).\n    At step total_steps the cosine term is -1, so the result is lr_min.\n    For step >= total_steps, clamp to lr_min (the floor).\n\n    Args:\n        step: int, the current step.\n        lr: float, the peak (maximum) learning rate.\n        total_steps: int, the total number of steps in the run.\n        lr_min: float, the floor the schedule decays to.\n    Returns:\n        float, the learning rate at this step.\n    \"\"\"\n    # TODO:\n    # 1. if step >= total_steps: return lr_min\n    # 2. otherwise return lr_min + 0.5 * (lr - lr_min) * (1 + cos(pi * step / total_steps))\n    raise NotImplementedError\n\n\ndef warmup_cosine_schedule(step, lr=0.01, total_steps=1000, warmup_steps=100,\n                           lr_min=1e-5, **kwargs):\n    \"\"\"Linear warmup for the first `warmup_steps`, then cosine decay to lr_min.\n\n    Warmup phase (step < warmup_steps):\n        lr(step) = lr * (step / warmup_steps)\n    Cosine phase (step >= warmup_steps):\n        progress = (step - warmup_steps) / (total_steps - warmup_steps)\n        lr(step) = lr_min + 0.5 * (lr - lr_min) * (1 + cos(pi * progress))\n\n    Warmup ramps from ~0 up to exactly `lr` at step == warmup_steps (the peak),\n    then cosine decay brings it down to lr_min at step == total_steps.\n    Edge case: if total_steps <= warmup_steps, stay in the warmup ramp the\n    whole time and return lr * (step / max(warmup_steps, 1)).\n\n    Args:\n        step: int, the current step.\n        lr: float, the peak learning rate reached at the end of warmup.\n        total_steps: int, the total number of steps.\n        warmup_steps: int, the number of linear-warmup steps.\n        lr_min: float, the floor for the cosine phase.\n    Returns:\n        float, the learning rate at this step.\n    \"\"\"\n    # TODO:\n    # 1. if total_steps <= warmup_steps: return lr * (step / max(warmup_steps, 1))\n    # 2. if step < warmup_steps: return lr * step / warmup_steps   (linear ramp)\n    # 3. else: progress = (step - warmup_steps) / (total_steps - warmup_steps)\n    #          return lr_min + 0.5 * (lr - lr_min) * (1 + cos(pi * progress))\n    raise NotImplementedError\n\n\ndef one_cycle_schedule(step, lr=0.01, total_steps=1000, **kwargs):\n    \"\"\"1cycle policy: ramp lr up in the first half, then down in the second half.\n\n    mid = max(total_steps // 2, 1)\n    Phase 1 (step < mid):  linear ramp from lr/25 up to lr\n        lr(step) = lr/25 + (lr - lr/25) * step / mid\n    Phase 2 (step >= mid): linear ramp from lr down to lr/10000\n        progress = (step - mid) / max(total_steps - mid, 1)\n        lr(step) = lr * (1 - progress) + (lr / 10000) * progress\n\n    Args:\n        step: int, the current step.\n        lr: float, the peak learning rate reached at the midpoint.\n        total_steps: int, the total number of steps.\n    Returns:\n        float, the learning rate at this step.\n    \"\"\"\n    # TODO:\n    # 1. mid = max(total_steps // 2, 1)\n    # 2. if step < mid: linear ramp from lr/25 to lr\n    # 3. else: linear ramp from lr down to lr/10000 over the remaining steps\n    raise NotImplementedError\n\n\ndef lr_curve(schedule_fn, total_steps, lr=0.01, **kwargs):\n    \"\"\"Sample a schedule at every step into a 1D np.ndarray of length total_steps.\n\n    Call schedule_fn(s, lr=lr, total_steps=total_steps, **kwargs) for\n    s = 0, 1, ..., total_steps - 1 and stack the results.\n\n    Args:\n        schedule_fn: one of the schedule functions above.\n        total_steps: int, the number of steps to sample.\n        lr: float, the base learning rate passed through to schedule_fn.\n        **kwargs: extra schedule-specific arguments (e.g. warmup_steps, gamma).\n    Returns:\n        np.ndarray of shape (total_steps,) and dtype float.\n    \"\"\"\n    # TODO: build a numpy array by calling schedule_fn for every step in range(total_steps)\n    raise NotImplementedError\n",
  "tests": "import math\nimport numpy as np\n\n# --- constant: same value everywhere ---\nassert constant_schedule(0, lr=0.05) == 0.05\nassert constant_schedule(999, lr=0.05) == 0.05\n_c = lr_curve(constant_schedule, 50, lr=0.03)\nassert _c.shape == (50,)\nassert np.allclose(_c, 0.03)\n\n# --- step decay: drops by gamma every step_size steps ---\nassert abs(step_decay_schedule(0, lr=0.1, step_size=10, gamma=0.1) - 0.1) < 1e-12\nassert abs(step_decay_schedule(9, lr=0.1, step_size=10, gamma=0.1) - 0.1) < 1e-12\nassert abs(step_decay_schedule(10, lr=0.1, step_size=10, gamma=0.1) - 0.01) < 1e-12\nassert abs(step_decay_schedule(25, lr=0.1, step_size=10, gamma=0.1) - 0.001) < 1e-12\n\n# --- cosine: starts at lr, hits the midpoint at lr/2, clamps past the end ---\nassert abs(cosine_schedule(0, lr=0.2, total_steps=100, lr_min=0.0) - 0.2) < 1e-9\n_mid = cosine_schedule(50, lr=0.2, total_steps=100, lr_min=0.0)\nassert abs(_mid - 0.1) < 1e-9  # cos(pi/2)=0 -> exactly halfway\nassert cosine_schedule(200, lr=0.2, total_steps=100, lr_min=0.01) == 0.01  # clamp past end\n_curve = lr_curve(cosine_schedule, 100, lr=0.2, lr_min=0.0)\nassert np.all(np.diff(_curve) <= 1e-12)  # never increases\n\n# --- warmup + cosine: linear ramp up then cosine down; peak exactly at warmup_steps ---\n_wc = lr_curve(warmup_cosine_schedule, 200, lr=0.1, warmup_steps=20, lr_min=0.0)\nassert abs(_wc[0]) < 1e-12              # starts at ~0\nassert _wc[10] > _wc[5] > _wc[0]        # ramping up during warmup\nassert abs(_wc[20] - 0.1) < 1e-9        # reaches peak lr exactly at warmup_steps\nassert int(np.argmax(_wc)) == 20        # peak is at the end of warmup\nassert _wc[-1] < _wc[20]               # decays after the peak\n\n# --- 1cycle: low start, peak at the midpoint, far-lower finish ---\n_oc = lr_curve(one_cycle_schedule, 100, lr=0.4)\nassert abs(_oc[0] - 0.4 / 25.0) < 1e-9   # starts at lr/25\n_peak = int(np.argmax(_oc))\nassert 49 <= _peak <= 50                  # peak around the midpoint\nassert _oc[_peak] >= 0.4 - 1e-6           # peak reaches ~lr\nassert _oc[-1] < _oc[0]                    # ends below the start (ramped far down)\n",
  "solution": "import math\nimport numpy as np\n\n\ndef constant_schedule(step, lr=0.01, **kwargs):\n    \"\"\"Constant learning rate: returns lr for every step.\"\"\"\n    return float(lr)\n\n\ndef step_decay_schedule(step, lr=0.1, step_size=100, gamma=0.1, **kwargs):\n    \"\"\"Step decay: lr * gamma ** floor(step / step_size).\"\"\"\n    return float(lr) * (float(gamma) ** (int(step) // int(step_size)))\n\n\ndef cosine_schedule(step, lr=0.01, total_steps=1000, lr_min=1e-5, **kwargs):\n    \"\"\"Cosine annealing from lr (at step 0) down to lr_min (at step total_steps).\"\"\"\n    if step >= total_steps:\n        return float(lr_min)\n    cos = math.cos(math.pi * step / total_steps)\n    return float(lr_min) + 0.5 * (float(lr) - float(lr_min)) * (1.0 + cos)\n\n\ndef warmup_cosine_schedule(step, lr=0.01, total_steps=1000, warmup_steps=100,\n                           lr_min=1e-5, **kwargs):\n    \"\"\"Linear warmup for the first warmup_steps, then cosine decay to lr_min.\"\"\"\n    if total_steps <= warmup_steps:\n        return float(lr) * (step / max(warmup_steps, 1))\n    if step < warmup_steps:\n        return float(lr) * step / warmup_steps\n    progress = (step - warmup_steps) / (total_steps - warmup_steps)\n    cos = math.cos(math.pi * progress)\n    return float(lr_min) + 0.5 * (float(lr) - float(lr_min)) * (1.0 + cos)\n\n\ndef one_cycle_schedule(step, lr=0.01, total_steps=1000, **kwargs):\n    \"\"\"1cycle policy: ramp lr/25 -> lr over the first half, then lr -> lr/10000.\"\"\"\n    lr = float(lr)\n    mid = max(total_steps // 2, 1)\n    if step < mid:\n        return (lr / 25.0) + (lr - lr / 25.0) * step / mid\n    progress = (step - mid) / max(total_steps - mid, 1)\n    return lr * (1.0 - progress) + (lr / 10000.0) * progress\n\n\ndef lr_curve(schedule_fn, total_steps, lr=0.01, **kwargs):\n    \"\"\"Sample a schedule over every step into a 1D np.ndarray of length total_steps.\"\"\"\n    vals = [schedule_fn(s, lr=lr, total_steps=total_steps, **kwargs)\n            for s in range(total_steps)]\n    return np.asarray(vals, dtype=float)\n"
}