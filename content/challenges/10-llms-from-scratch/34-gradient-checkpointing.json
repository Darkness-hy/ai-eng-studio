{
  "titleZh": "梯度检查点：重算换显存",
  "titleEn": "Gradient Checkpointing: Trade Compute for Memory",
  "promptZh": "训练时反向传播需要每一层的输入激活，显存开销巨大。梯度检查点的做法是：前向只保存每个“段”（k 层为一段）的输入，丢弃段内的中间激活；反向时再从保存的段输入重算该段前向，恢复出梯度所需的中间量，从而用算力换显存。\\n\\n请实现两个函数：\\n1. model_forward_checkpointed(x, params, k)：完整跑前向，但只保存每个段的输入。段数为 ceil(len(params)/k)，因此 saved_inputs 长度等于段数，且 saved_inputs[0] 必须是 x，不要把最终输出额外追加进去。\\n2. model_backward_checkpointed(grad_output, saved_inputs, params, k)：从后往前逐段处理，用 model_forward 从保存的段输入重算该段激活，再用 model_backward 求该段梯度，并把梯度沿段间传递回去。\\n\\n正确性标准：得到的参数梯度必须与对完整激活列表做 model_backward 的结果逐元素相同（机器精度内）。scaffolding（relu / layer_forward / layer_backward / model_forward / model_backward）已给出，请勿修改，直接调用。只能用 numpy 与标准库。",
  "promptEn": "Backprop needs the input activation of every layer, which is memory-hungry. Gradient checkpointing fixes this: during forward, save only the INPUT to each segment (k layers per segment) and drop the intermediates inside a segment; during backward, recompute each segment's forward from its saved input to materialize the tensors gradients need. You trade compute for memory.\\n\\nImplement two functions:\\n1. model_forward_checkpointed(x, params, k): run the full forward but save only each segment's input. There are ceil(len(params)/k) segments, so len(saved_inputs) equals that count, saved_inputs[0] must be x, and you must NOT append the final output as an extra entry.\\n2. model_backward_checkpointed(grad_output, saved_inputs, params, k): process segments from last to first; recompute a segment's activations with model_forward from its saved input, run model_backward over the segment, and thread the gradient between segments.\\n\\nCorrectness bar: the resulting parameter gradients must be element-wise identical (to machine precision) to model_backward run on the full activation list. The scaffolding (relu / layer_forward / layer_backward / model_forward / model_backward) is provided -- do not modify it, just call it. Use only numpy and the standard library.",
  "starter": "import numpy as np\n\n\n# ---- Given scaffolding: a 2-layer-MLP \"transformer layer\" and its backward.\n# You do NOT need to change anything below this block.\ndef relu(x):\n    return np.maximum(x, 0.0)\n\n\ndef layer_forward(x, w1, b1, w2, b2):\n    \"\"\"One layer: out = relu(x @ w1 + b1) @ w2 + b2.\"\"\"\n    h = relu(x @ w1 + b1)\n    return h @ w2 + b2\n\n\ndef layer_backward(g, x_in, w1, b1, w2, b2):\n    \"\"\"Recompute the layer from x_in, then backprop. Returns (grad_x, grads).\"\"\"\n    h_pre = x_in @ w1 + b1\n    h = relu(h_pre)\n    gw2 = h.T @ g\n    gb2 = g.sum(axis=0)\n    gh = g @ w2.T\n    g_pre = gh * (h_pre > 0)\n    gw1 = x_in.T @ g_pre\n    gb1 = g_pre.sum(axis=0)\n    gx = g_pre @ w1.T\n    return gx, (gw1, gb1, gw2, gb2)\n\n\ndef model_forward(x, params):\n    \"\"\"Plain forward that keeps EVERY layer input (the memory-hungry baseline).\"\"\"\n    activations = [x]\n    h = x\n    for w1, b1, w2, b2 in params:\n        h = layer_forward(h, w1, b1, w2, b2)\n        activations.append(h)\n    return h, activations\n\n\ndef model_backward(grad_output, activations, params):\n    \"\"\"Plain backward using the full activation list from model_forward.\"\"\"\n    grads = [None] * len(params)\n    g = grad_output\n    for i in range(len(params) - 1, -1, -1):\n        w1, b1, w2, b2 = params[i]\n        g, grads[i] = layer_backward(g, activations[i], w1, b1, w2, b2)\n    return g, grads\n\n\n# ====================  IMPLEMENT THE TWO FUNCTIONS BELOW  ====================\n\ndef model_forward_checkpointed(x, params, k=2):\n    \"\"\"Gradient-checkpointing forward.\n\n    Split the `params` list into segments of `k` consecutive layers. Run the\n    full forward pass, but SAVE ONLY the input to each segment (the activations\n    *inside* a segment are dropped to save memory; they will be recomputed in\n    the backward pass).\n\n    Args:\n        x: input array, shape (batch, hidden).\n        params: list of (w1, b1, w2, b2) tuples, one per layer.\n        k: segment size (number of layers per checkpoint segment), k >= 1.\n\n    Returns:\n        (out, saved_inputs) where:\n          - out is the final model output (identical to model_forward's output).\n          - saved_inputs is a list with one array per segment: the input fed\n            into that segment. saved_inputs[0] must be x. There are\n            ceil(len(params) / k) segments, so len(saved_inputs) equals that\n            count. Do NOT append the final output as an extra entry.\n\n    Hint: walk layers in order; the input to segment j is the activation right\n    before layer index j*k. Save it the moment you reach a segment boundary.\n    \"\"\"\n    # TODO: implement checkpointed forward.\n    raise NotImplementedError\n\n\ndef model_backward_checkpointed(grad_output, saved_inputs, params, k=2):\n    \"\"\"Gradient-checkpointing backward.\n\n    Walk segments from last to first. For each segment, RECOMPUTE its forward\n    activations from the saved segment input (call model_forward on that\n    segment's params), then run model_backward over the segment to get the\n    per-layer gradients and the gradient flowing into the segment input.\n\n    The resulting parameter gradients must be IDENTICAL (to machine precision)\n    to model_backward run on the full activation list.\n\n    Args:\n        grad_output: gradient w.r.t. the final model output.\n        saved_inputs: the list returned by model_forward_checkpointed.\n        params: list of (w1, b1, w2, b2) tuples, one per layer.\n        k: segment size used in the forward pass.\n\n    Returns:\n        (grad_x, grads) where grad_x is the gradient w.r.t. x and grads is a\n        list of per-layer gradient tuples aligned with `params`.\n\n    Hint: there are ceil(len(params) / k) segments; segment j covers layer\n    indices [j*k, min((j+1)*k, len(params))). Process them in reverse, threading\n    the gradient from one segment's input into the previous segment's output.\n    \"\"\"\n    # TODO: implement checkpointed backward.\n    raise NotImplementedError\n",
  "tests": "import numpy as np\n\n# Small matmuls on some platforms emit spurious FP warnings from the BLAS\n# SIMD tail; results are correct. Silence them so output stays clean.\nnp.seterr(all=\"ignore\")\n\n\ndef _make_params(n_layers, hidden, inner, seed=0):\n    rng = np.random.default_rng(seed)\n    params = []\n    for _ in range(n_layers):\n        w1 = (rng.standard_normal((hidden, inner)) / np.sqrt(hidden))\n        b1 = rng.standard_normal(inner) * 0.1\n        w2 = (rng.standard_normal((inner, hidden)) / np.sqrt(inner))\n        b2 = rng.standard_normal(hidden) * 0.1\n        params.append((w1, b1, w2, b2))\n    return params\n\n\ndef _max_grad_diff(ga, gb):\n    d = 0.0\n    for la, lb in zip(ga, gb):\n        for a, b in zip(la, lb):\n            d = max(d, float(np.max(np.abs(a - b))))\n    return d\n\n\n# Test 1: checkpointed forward output equals the full-forward output.\nrng = np.random.default_rng(7)\nparams = _make_params(6, 8, 16)\nx = rng.standard_normal((4, 8))\nout_full, _ = model_forward(x, params)\nout_ck, saved = model_forward_checkpointed(x, params, k=2)\nassert np.allclose(out_full, out_ck, atol=1e-9)\n\n# Test 2: forward saves exactly one input per segment, and the first is x.\nassert len(saved) == (6 + 2 - 1) // 2  # 3 segments -> 3 saved inputs\nassert np.allclose(saved[0], x)\n\n# Test 3: checkpointed gradients match the full backward (k=2).\ngrad_out = rng.standard_normal(out_full.shape)\n_, grads_full = model_backward(grad_out, model_forward(x, params)[1], params)\n_, grads_ck = model_backward_checkpointed(grad_out, saved, params, k=2)\nassert _max_grad_diff(grads_full, grads_ck) < 1e-8\n\n# Test 4: k=1 (checkpoint every layer) saves one input per layer and matches.\nout_ck1, saved1 = model_forward_checkpointed(x, params, k=1)\nassert len(saved1) == 6\n_, grads_ck1 = model_backward_checkpointed(grad_out, saved1, params, k=1)\nassert _max_grad_diff(grads_full, grads_ck1) < 1e-8\n\n# Test 5: k >= n_layers (single segment) saves only x and still matches.\nout_ckN, savedN = model_forward_checkpointed(x, params, k=10)\nassert len(savedN) == 1\n_, grads_ckN = model_backward_checkpointed(grad_out, savedN, params, k=10)\nassert _max_grad_diff(grads_full, grads_ckN) < 1e-8\n\n# Test 6: non-divisible case n_layers=5, k=2 -> segments of size 2, 2, 1.\nparams5 = _make_params(5, 6, 10, seed=3)\nx5 = rng.standard_normal((3, 6))\nout5, _ = model_forward(x5, params5)\ng5 = rng.standard_normal(out5.shape)\n_, gf5 = model_backward(g5, model_forward(x5, params5)[1], params5)\no5, s5 = model_forward_checkpointed(x5, params5, k=2)\nassert len(s5) == 3 and np.allclose(o5, out5, atol=1e-9)\n_, gc5 = model_backward_checkpointed(g5, s5, params5, k=2)\nassert _max_grad_diff(gf5, gc5) < 1e-8\n",
  "solution": "import numpy as np\n\n\n# ---- Given scaffolding: a 2-layer-MLP \"transformer layer\" and its backward.\n# You do NOT need to change anything below this block.\ndef relu(x):\n    return np.maximum(x, 0.0)\n\n\ndef layer_forward(x, w1, b1, w2, b2):\n    \"\"\"One layer: out = relu(x @ w1 + b1) @ w2 + b2.\"\"\"\n    h = relu(x @ w1 + b1)\n    return h @ w2 + b2\n\n\ndef layer_backward(g, x_in, w1, b1, w2, b2):\n    \"\"\"Recompute the layer from x_in, then backprop. Returns (grad_x, grads).\"\"\"\n    h_pre = x_in @ w1 + b1\n    h = relu(h_pre)\n    gw2 = h.T @ g\n    gb2 = g.sum(axis=0)\n    gh = g @ w2.T\n    g_pre = gh * (h_pre > 0)\n    gw1 = x_in.T @ g_pre\n    gb1 = g_pre.sum(axis=0)\n    gx = g_pre @ w1.T\n    return gx, (gw1, gb1, gw2, gb2)\n\n\ndef model_forward(x, params):\n    \"\"\"Plain forward that keeps EVERY layer input (the memory-hungry baseline).\"\"\"\n    activations = [x]\n    h = x\n    for w1, b1, w2, b2 in params:\n        h = layer_forward(h, w1, b1, w2, b2)\n        activations.append(h)\n    return h, activations\n\n\ndef model_backward(grad_output, activations, params):\n    \"\"\"Plain backward using the full activation list from model_forward.\"\"\"\n    grads = [None] * len(params)\n    g = grad_output\n    for i in range(len(params) - 1, -1, -1):\n        w1, b1, w2, b2 = params[i]\n        g, grads[i] = layer_backward(g, activations[i], w1, b1, w2, b2)\n    return g, grads\n\n\n# ====================  REFERENCE SOLUTION  ====================\n\ndef model_forward_checkpointed(x, params, k=2):\n    saved_inputs = [x]\n    h = x\n    for i, (w1, b1, w2, b2) in enumerate(params):\n        h = layer_forward(h, w1, b1, w2, b2)\n        if (i + 1) % k == 0 and (i + 1) < len(params):\n            saved_inputs.append(h)\n    return h, saved_inputs\n\n\ndef model_backward_checkpointed(grad_output, saved_inputs, params, k=2):\n    grads = [None] * len(params)\n    g = grad_output\n    n_seg = (len(params) + k - 1) // k\n    for seg_idx in range(n_seg - 1, -1, -1):\n        start = seg_idx * k\n        end = min(start + k, len(params))\n        x_in = saved_inputs[seg_idx]\n        _, seg_acts = model_forward(x_in, params[start:end])\n        g, seg_grads = model_backward(g, seg_acts, params[start:end])\n        for j, gr in enumerate(seg_grads):\n            grads[start + j] = gr\n    return g, grads\n"
}