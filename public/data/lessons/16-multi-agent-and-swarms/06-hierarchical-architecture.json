{"id":"16-multi-agent-and-swarms/06-hierarchical-architecture","phase":"16-multi-agent-and-swarms","slug":"06-hierarchical-architecture","title":"Hierarchical Architecture and Its Failure Mode","titleZh":"层级架构及其失效模式","quote":"Hierarchical is supervisor nested. Manager agents over sub-managers over workers. CrewAI `Process.hierarchical` is the textbook version: a `manager_llm` dynamically delegates tasks and validates outputs. The LangGraph equivalent is `create_supervisor(create_supervisor(...))`. It is the natural pattern when the task is a real org chart. It is also the pattern most likely to collapse into managerial looping — manager agents assign work poorly, misinterpret sub-outputs, or fail to reach consensus. Sequential often beats it.","meta":{"type":"Learn + Build","languages":"Python (stdlib)","prerequisites":"Phase 16 · 05 (Supervisor Pattern)","time":"~60 minutes"},"bodyEn":"# Hierarchical Architecture and Its Failure Mode\n\n> Hierarchical is supervisor nested. Manager agents over sub-managers over workers. CrewAI `Process.hierarchical` is the textbook version: a `manager_llm` dynamically delegates tasks and validates outputs. The LangGraph equivalent is `create_supervisor(create_supervisor(...))`. It is the natural pattern when the task is a real org chart. It is also the pattern most likely to collapse into managerial looping — manager agents assign work poorly, misinterpret sub-outputs, or fail to reach consensus. Sequential often beats it.\n\n**Type:** Learn + Build\n**Languages:** Python (stdlib)\n**Prerequisites:** Phase 16 · 05 (Supervisor Pattern)\n**Time:** ~60 minutes\n\n## Problem\n\nOnce the supervisor pattern clicks, the natural next step is \"what if the workers are themselves supervisors?\" Teams have sub-teams; companies have departments of departments. Hierarchical architectures mirror that.\n\nThe issue: LLM managers are not the same as human managers. A human manager has stable priors about what their reports know. An LLM manager re-reasons the org every turn from whatever is in its context. Tiny drift in that context, and the whole tree misallocates work.\n\n## Concept\n\n### The shape\n\n```\n                 Manager\n                 ┌─────┐\n                 └──┬──┘\n           ┌────────┴────────┐\n           ▼                 ▼\n       Sub-Mgr A         Sub-Mgr B\n       ┌─────┐           ┌─────┐\n       └──┬──┘           └──┬──┘\n         ┌┴──┬──┐          ┌┴──┐\n         ▼   ▼  ▼          ▼   ▼\n       W1  W2  W3         W4  W5\n```\n\nEvery internal node plans, delegates, and synthesizes. Only leaves do work.\n\n### Where it shines\n\n- **Clear org mapping.** If the real task is departmental (\"legal review the doc, finance review the doc, engineering review the doc, then summarize for exec\"), the hierarchy is explicit.\n- **Local summarization.** Each sub-manager synthesizes its team's output before the top manager sees it. Top manager sees three sub-manager summaries, not fifteen worker outputs.\n\n### Where it breaks\n\nThree failure modes the 2026 post-mortems keep finding:\n\n1. **Task assignment error.** The manager reads the goal, hallucinates a decomposition, and delegates to the wrong sub-manager. Because the sub-manager obediently works on what it was given, the error only surfaces at the top synthesis — one level removed from where a human could have caught it.\n2. **Output misinterpretation.** Sub-manager returns \"unable to verify claim X.\" Top manager summarizes as \"claim X not confirmed.\" Meaning drifts at every level.\n3. **Consensus loops.** Two sub-managers disagree; top manager asks them to reconcile; they re-delegate down; workers re-run; sub-managers return slightly different answers; loop. CrewAI's `Process.hierarchical` guards against this with step limits, but the limit itself is now a hyperparameter.\n\n### The deciding question\n\nSequential (linear pipeline) vs hierarchical: does your task actually have independent sub-teams, or is it one linear flow pretending to be a tree? If the latter, use sequential. If the former, use hierarchical but budget explicit reconciliation rules.\n\n### CrewAI's implementation\n\n`Process.hierarchical` wires a manager LLM over specialist crews. The manager:\n\n- receives the top-level task,\n- assigns subtasks to crews,\n- evaluates crew outputs,\n- decides whether to accept, re-delegate, or iterate.\n\nDocumentation: https://docs.crewai.com/en/introduction (look for \"Hierarchical Process\" under Core Concepts).\n\n### LangGraph's implementation\n\nLangGraph uses nested `create_supervisor` calls. The inner supervisor has its own graph; the outer supervisor treats the inner graph as an opaque node. This is cleaner than CrewAI for debugging (you can step through each graph separately) but harder to express dynamic reshaping of the tree.\n\nReference: https://reference.langchain.com/python/langgraph-supervisor.\n\n## Build It\n\n`code/main.py` runs a 3-level hierarchy:\n\n- top manager: splits a task into \"engineering\" and \"legal\" branches,\n- engineering sub-manager: splits into \"frontend\" and \"backend\" workers,\n- legal sub-manager: one worker.\n\nDemo contrasts happy path (everyone agrees) against a **perturbed path** where the top manager's decomposition mislabels \"legal\" as \"finance\" and watches the error cascade — the sub-manager obediently does finance work, the top synthesizer reports finance findings, the original legal question goes unanswered.\n\nRun:\n\n```\npython3 code/main.py\n```\n\nOutput shows both paths with a clear side-by-side of \"what was asked\" vs \"what was delivered.\"\n\n## Use It\n\n`outputs/skill-hierarchy-fitness.md` evaluates whether a given task should use hierarchical, sequential, or flat supervisor. Inputs: task description, org structure, reconciliation budget. Output: pattern recommendation with the specific failure modes to guard against.\n\n## Ship It\n\nIf you ship hierarchical:\n\n- **Cap tree depth at 2.** Three levels already hides most errors from observability.\n- **Explicit reconciliation budget.** Set max rounds before the top manager must commit. Usually 2.\n- **Provenance on every synthesis.** Each node's summary must cite which leaf outputs produced it.\n- **Alert on decomposition drift.** Log the manager's decomposition per step; diff against the user query. If the decomposition no longer covers the query, fire an alert.\n\n## Exercises\n\n1. Run `code/main.py` and compare happy vs perturbed. How many levels of manager hand-off does it take before the top output fully diverges from the user's question?\n2. Add a third level (top → sub → sub-sub → worker). Measure how often the perturbed path corrects itself vs fully diverges as depth grows.\n3. Implement a \"canary\" worker at each sub-manager that is always asked the original user question unchanged. Use the canary answer to detect decomposition drift. How should the manager react when the canary disagrees with the synthesized answer?\n4. Read CrewAI's `Process.hierarchical` docs. Identify one concrete guardrail CrewAI applies (step limit, manager_llm constraint) and describe what failure mode it targets.\n5. Compare nested LangGraph supervisors to CrewAI hierarchical. Which makes reconciliation loops cheaper to detect?\n\n## Key Terms\n\n| Term | What people say | What it actually means |\n|------|----------------|------------------------|\n| Hierarchical | \"Org chart pattern\" | Supervisors over supervisors; only leaves do work. |\n| Manager LLM | \"The boss\" | The LLM that decomposes, assigns, and validates at an internal node. |\n| Decomposition drift | \"The boss lost the plot\" | Top manager's split no longer covers the original question. |\n| Reconciliation loop | \"Endless meetings\" | Sub-managers disagree; top re-delegates; workers re-run; loop until budget exhausted. |\n| Depth-2 ceiling | \"Don't go deeper than 2 levels\" | Empirical guardrail: 3+ levels collapses observability. |\n| Canary question | \"Ground truth at every level\" | A worker that is always asked the original query unchanged, to detect drift. |\n| Provenance chain | \"Who said what\" | Trace from each synthesis back to the leaf outputs that produced it. |\n\n## Further Reading\n\n- [CrewAI introduction — Process.hierarchical](https://docs.crewai.com/en/introduction) — textbook hierarchical with a manager LLM\n- [LangGraph supervisor reference](https://reference.langchain.com/python/langgraph-supervisor) — nested supervisor via `create_supervisor`\n- [Anthropic engineering — Research system](https://www.anthropic.com/engineering/multi-agent-research-system) — why Anthropic deliberately chose flat supervisor over hierarchical\n- [Cemri et al. — Why Do Multi-Agent LLM Systems Fail?](https://arxiv.org/abs/2503.13657) — MAST taxonomy; section on coordination failures documents decomposition drift\n","bodyZh":"# 层级式架构及其失效模式\n\n> 层级式（hierarchical）就是嵌套的监督者：管理者智能体之下是子管理者，子管理者之下是工作者。CrewAI 的 `Process.hierarchical` 是教科书式的实现：由一个 `manager_llm` 动态分派任务并校验输出。LangGraph 的对应写法是 `create_supervisor(create_supervisor(...))`。当任务本身就是一张真实的组织架构图时，这是最自然的模式。但它也是最容易陷入\"管理层空转\"的模式——管理者智能体分派任务不当、误读下级输出、或迟迟无法达成共识。很多时候顺序式（sequential）反而更胜一筹。\n\n**Type:** Learn + Build\n**Languages:** Python (stdlib)\n**Prerequisites:** Phase 16 · 05 (Supervisor Pattern)\n**Time:** ~60 minutes\n\n## 问题背景\n\n一旦理解了监督者模式，自然会想到下一步：\"如果工作者本身也是监督者呢？\"团队有子团队，公司有部门套部门。层级式架构正是对这种结构的映射。\n\n问题在于：LLM 管理者和人类管理者并不一样。人类管理者对下属的能力有稳定的先验认知，而 LLM 管理者每一轮都要根据上下文中的内容重新推理整个组织结构。上下文只要出现细微漂移，整棵树的任务分配就会出错。\n\n## 核心概念\n\n### 结构形态\n\n```\n                 Manager\n                 ┌─────┐\n                 └──┬──┘\n           ┌────────┴────────┐\n           ▼                 ▼\n       Sub-Mgr A         Sub-Mgr B\n       ┌─────┐           ┌─────┐\n       └──┬──┘           └──┬──┘\n         ┌┴──┬──┐          ┌┴──┐\n         ▼   ▼  ▼          ▼   ▼\n       W1  W2  W3         W4  W5\n```\n\n每个内部节点都负责规划、分派和汇总，只有叶子节点才真正干活。\n\n### 适用场景\n\n- **组织结构映射清晰。** 如果真实任务本身就是按部门划分的（\"法务审一遍文档，财务审一遍文档，工程审一遍文档，最后给高管做总结\"），层级结构是显而易见的。\n- **局部汇总。** 每个子管理者先把自己团队的输出汇总好，再交给顶层管理者。顶层管理者只看到三份子管理者摘要，而不是十五份工作者输出。\n\n### 失效场景\n\n2026 年的各类事故复盘反复发现三种失效模式：\n\n1. **任务分派错误。** 管理者读完目标后，凭空臆造出一种任务分解方式，把任务派给了错误的子管理者。由于子管理者会老老实实地执行分到手的任务，错误只会在顶层汇总时才暴露出来——而那已经离人类本可以发现问题的位置隔了一层。\n2. **输出误读。** 子管理者返回\"无法核实论断 X\"，顶层管理者却总结成\"论断 X 未获确认\"。语义在每一层都在漂移。\n3. **共识循环。** 两个子管理者意见不一；顶层管理者要求它们协调；它们再向下重新分派；工作者重新执行；子管理者返回略有差异的新答案；循环往复。CrewAI 的 `Process.hierarchical` 用步数上限来防范这种情况，但这个上限本身又成了一个新的超参数。\n\n### 决定性问题\n\n顺序式（线性流水线）还是层级式：你的任务是真的存在相互独立的子团队，还是一条线性流程伪装成了一棵树？如果是后者，用顺序式。如果是前者，可以用层级式，但要预先制定明确的协调规则。\n\n### CrewAI 的实现\n\n`Process.hierarchical` 在多个专家 crew 之上接入一个管理者 LLM。该管理者：\n\n- 接收顶层任务，\n- 向各 crew 分派子任务，\n- 评估各 crew 的输出，\n- 决定是接受、重新分派，还是再迭代一轮。\n\n文档：https://docs.crewai.com/en/introduction（在 Core Concepts 下查找 \"Hierarchical Process\"）。\n\n### LangGraph 的实现\n\nLangGraph 使用嵌套的 `create_supervisor` 调用。内层监督者拥有自己的图；外层监督者把内层的图当作一个不透明节点来对待。这种方式在调试上比 CrewAI 更清爽（你可以分别单步调试每张图），但更难表达对树结构的动态重组。\n\n参考：https://reference.langchain.com/python/langgraph-supervisor。\n\n## 从零实现\n\n`code/main.py` 运行一个三层的层级结构：\n\n- 顶层管理者：把任务拆成\"工程\"和\"法务\"两个分支，\n- 工程子管理者：再拆成\"前端\"和\"后端\"两个工作者，\n- 法务子管理者：只有一个工作者。\n\n演示对比了正常路径（各方一致）和一条**扰动路径**：顶层管理者在任务分解时把\"法务\"误标成了\"财务\"，然后观察错误如何层层级联——子管理者老老实实地做了财务方面的工作，顶层汇总者报告的是财务结论，而最初的法务问题无人回答。\n\n运行：\n\n```\npython3 code/main.py\n```\n\n输出会展示两条路径，并清晰地并排对比\"用户问的是什么\"与\"实际交付了什么\"。\n\n## 生产实践\n\n`outputs/skill-hierarchy-fitness.md` 用于评估给定任务应该采用层级式、顺序式还是扁平监督者模式。输入：任务描述、组织结构、协调预算。输出：模式推荐，以及需要重点防范的具体失效模式。\n\n## 交付产物\n\n如果你要将层级式架构投入生产：\n\n- **树深度上限设为 2。** 三层结构已经会让大部分错误逃出可观测性的覆盖范围。\n- **明确的协调预算。** 设定顶层管理者必须拍板前的最大协调轮数，通常为 2。\n- **每次汇总都带溯源信息。** 每个节点的摘要必须注明它由哪些叶子输出生成。\n- **对分解漂移设置告警。** 逐步记录管理者的任务分解结果，与用户查询做差异比对。一旦分解不再覆盖原始查询，立即触发告警。\n\n## 练习\n\n1. 运行 `code/main.py`，对比正常路径与扰动路径。经过多少层管理者交接之后，顶层输出会与用户的问题完全脱节？\n2. 增加第三层（顶层 → 子层 → 子子层 → 工作者）。测量随着深度增加，扰动路径自我纠正与完全偏离的比例各是多少。\n3. 在每个子管理者下实现一个\"金丝雀\"工作者，始终原封不动地向它提出用户最初的问题。用金丝雀的答案来检测分解漂移。当金丝雀的答案与汇总答案不一致时，管理者应该如何应对？\n4. 阅读 CrewAI 的 `Process.hierarchical` 文档。找出 CrewAI 应用的一个具体护栏（步数上限、manager_llm 约束），并说明它针对的是哪种失效模式。\n5. 对比嵌套的 LangGraph 监督者与 CrewAI 层级模式。哪一种能让协调循环更容易被检测出来？\n\n## 关键术语\n\n| 术语 | 大家怎么说 | 实际含义 |\n|------|----------------|------------------------|\n| 层级式（Hierarchical） | \"组织架构图模式\" | 监督者之上还有监督者；只有叶子节点干活。 |\n| 管理者 LLM（Manager LLM） | \"老板\" | 在内部节点上负责分解、分派和校验的 LLM。 |\n| 分解漂移（Decomposition drift） | \"老板跑题了\" | 顶层管理者的任务拆分不再覆盖原始问题。 |\n| 协调循环（Reconciliation loop） | \"没完没了的会议\" | 子管理者意见不一；顶层重新分派；工作者重新执行；循环直到预算耗尽。 |\n| 深度 2 上限（Depth-2 ceiling） | \"别超过两层\" | 经验性护栏：三层及以上会让可观测性崩塌。 |\n| 金丝雀问题（Canary question） | \"每一层都有基准真值\" | 始终被原封不动地问到原始查询的工作者，用于检测漂移。 |\n| 溯源链（Provenance chain） | \"谁说了什么\" | 从每次汇总回溯到生成它的叶子输出的追踪链路。 |\n\n## 延伸阅读\n\n- [CrewAI introduction — Process.hierarchical](https://docs.crewai.com/en/introduction) — 带管理者 LLM 的教科书式层级实现\n- [LangGraph supervisor reference](https://reference.langchain.com/python/langgraph-supervisor) — 通过 `create_supervisor` 实现嵌套监督者\n- [Anthropic engineering — Research system](https://www.anthropic.com/engineering/multi-agent-research-system) — Anthropic 为什么刻意选择扁平监督者而不是层级式\n- [Cemri et al. — Why Do Multi-Agent LLM Systems Fail?](https://arxiv.org/abs/2503.13657) — MAST 失效分类法；其中关于协调失效的章节记录了分解漂移现象\n","quizEn":null,"quizZh":null,"code":[{"name":"main.py","lang":"python","content":"\"\"\"Hierarchical multi-agent with decomposition-drift demo.\n\n3-level hierarchy: top manager -> sub-managers -> workers.\nRun happy path and a perturbed path where the top manager mislabels one branch.\nWatch the error cascade.\n\"\"\"\nfrom __future__ import annotations\n\nfrom dataclasses import dataclass, field\n\n\n@dataclass\nclass LeafOutput:\n    worker: str\n    question: str\n    answer: str\n\n\n@dataclass\nclass SubSummary:\n    sub_manager: str\n    leaves: list[LeafOutput]\n    summary: str\n\n\n@dataclass\nclass TopSynthesis:\n    top_manager: str\n    branches: list[SubSummary]\n    synthesis: str\n\n\nclass Worker:\n    def __init__(self, name: str, canned: dict[str, str]) -> None:\n        self.name = name\n        self.canned = canned\n\n    def run(self, question: str) -> LeafOutput:\n        key = self._match_key(question)\n        ans = self.canned.get(key, f\"[no canned answer for '{question}']\")\n        return LeafOutput(worker=self.name, question=question, answer=ans)\n\n    def _match_key(self, q: str) -> str:\n        ql = q.lower()\n        for k in self.canned:\n            if k in ql:\n                return k\n        return \"default\"\n\n\nclass SubManager:\n    def __init__(self, name: str, workers: list[Worker], split: dict[str, str]) -> None:\n        self.name = name\n        self.workers = workers\n        self.split = split\n\n    def run(self, task: str) -> SubSummary:\n        leaves = []\n        for w in self.workers:\n            sub_q = self.split.get(w.name, task)\n            leaves.append(w.run(sub_q))\n        summary = f\"[{self.name}] aggregated: \" + \" | \".join(l.answer for l in leaves)\n        return SubSummary(sub_manager=self.name, leaves=leaves, summary=summary)\n\n\nclass TopManager:\n    def __init__(self, name: str, subs: dict[str, SubManager]) -> None:\n        self.name = name\n        self.subs = subs\n\n    def run(self, task: str, branch_labels: list[str]) -> TopSynthesis:\n        summaries: list[SubSummary] = []\n        for label in branch_labels:\n            if label not in self.subs:\n                summaries.append(\n                    SubSummary(\n                        sub_manager=f\"MISSING[{label}]\",\n                        leaves=[],\n                        summary=f\"[top] tried to delegate to '{label}' -- no such sub-manager\",\n                    )\n                )\n                continue\n            summaries.append(self.subs[label].run(f\"{task} -- branch: {label}\"))\n        synth = \"top synthesis: \" + \" || \".join(s.summary for s in summaries)\n        return TopSynthesis(top_manager=self.name, branches=summaries, synthesis=synth)\n\n\ndef build_hierarchy() -> TopManager:\n    fe = Worker(\"fe\", {\"frontend\": \"React component audited; 2 issues.\"})\n    be = Worker(\"be\", {\"backend\": \"API endpoints audited; 1 issue.\"})\n    eng = SubManager(\n        \"eng-manager\",\n        [fe, be],\n        {\"fe\": \"frontend review of the feature\", \"be\": \"backend review of the feature\"},\n    )\n    lw = Worker(\"lawyer\", {\"legal\": \"Contract clauses A and B are non-compliant.\"})\n    legal = SubManager(\"legal-manager\", [lw], {\"lawyer\": \"legal review of the feature\"})\n    fw = Worker(\n        \"finance\",\n        {\"finance\": \"Projected cost $42k/month; exceeds budget by 12%.\"},\n    )\n    finance = SubManager(\"finance-manager\", [fw], {\"finance\": \"finance review of the feature\"})\n    return TopManager(\"vp-eng\", {\"engineering\": eng, \"legal\": legal, \"finance\": finance})\n\n\ndef render(label: str, synth: TopSynthesis) -> None:\n    print(f\"\\n=== {label} ===\")\n    for branch in synth.branches:\n        print(f\"  [sub] {branch.sub_manager}\")\n        for leaf in branch.leaves:\n            print(f\"    [leaf] {leaf.worker:8s} asked: {leaf.question}\")\n            print(f\"           answered: {leaf.answer}\")\n        print(f\"    [summary] {branch.summary}\")\n    print(f\"  [top] {synth.synthesis}\")\n\n\ndef main() -> None:\n    print(\"Hierarchical multi-agent with decomposition-drift demo\")\n    print(\"-\" * 60)\n\n    top = build_hierarchy()\n    task = \"Ship the premium tier feature to production.\"\n\n    happy = top.run(task, branch_labels=[\"engineering\", \"legal\"])\n    render(\"Happy path (correct branches)\", happy)\n\n    perturbed = top.run(task, branch_labels=[\"engineering\", \"finance\"])\n    render(\"Perturbed path (top manager mislabels 'legal' as 'finance')\", perturbed)\n\n    print(\"\\nUser asked about legal/engineering review.\")\n    print(\"Happy path: both legal and engineering answer truthfully.\")\n    print(\"Perturbed path: finance dutifully answers, legal question goes unanswered.\")\n    print(\"The error appears at TOP synthesis -- one level removed from where a human could catch it.\")\n\n\nif __name__ == \"__main__\":\n    main()\n","runnable":false}]}